Quickest way to clone and run tests from this repo:

- Download Node and Visual Studio Code
- create a package.json file
- initialize the folder
   npm init -y
- Install cypress: 
npm install cypress
- open cypress
  npx cypress open
